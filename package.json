{
  "dependencies": {
    "sharp": "^0.29.1"
  },
  "scripts": {
    "predeploy": "rm -rf node_modules/sharp && npm install --platform=linux --arch=x64 --target=12.13.0 sharp",
    "deploy": "serverless deploy --verbose",
    "postdeploy": "rm -rf node_modusles && npm install sharp"
  }
}
